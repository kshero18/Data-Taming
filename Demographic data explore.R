
##### Exploring data

stats


nrow(stats) #no of rows
ncol(stats) #no of columns

head(stats) #top 5/6 rows
head(stats,n=10)
tail(stats) #bottom 6 rows
tail(stats,n=8)

str(stats) #type and info on the data str() (structure) and runif() (random uniformly : r unif)
summary(stats) #breakdown for the data
